<%= form_for(@home_team) do |f| %>
  <% if @home_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_team.errors.count, "error") %> prohibited this home_team from being saved:</h2>

      <ul>
      <% @home_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
