<h1>Listing home_teams</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @our_teams.each do |home_team| %>
  <tr>
    <td><%= home_team.name %></td>
    <td><%= link_to 'Show', home_team %></td>
    <td><%= link_to 'Edit', edit_home_team_path(home_team) %></td>
    <td><%= link_to 'Destroy', home_team, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Home team', new_home_team_path %>
